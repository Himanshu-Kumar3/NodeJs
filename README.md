# This is a repo for node js

# NodeJS 
  - JS runtime environment 
  - Builts on the google chrome's V8 engine
  - runs JS outside of the browser
  


 # To initialize node 
  - Download and install node
  - For window install (Git bash) terminal
  - type - node to initialize
  
 
 

 # Global Object
   - global 
     -  global object
   - globalThis 
     - central global object
     - points to global object in all the browser (V8 ,nodeJS ...)